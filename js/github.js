async function ghGetFile({token,config,path}){const url=`https://api.github.com/repos/${config.owner}/${config.repo}/contents/${path}?ref=${config.branch}`;const r=await fetch(url,{headers:{Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error(`GET ${path}: ${r.status}`);return r.json()}
async function ghPutFile({token,config,path,content,message,sha}){const url=`https://api.github.com/repos/${config.owner}/${config.repo}/contents/${path}`;const body={message,content:btoa(unescape(encodeURIComponent(content))),branch:config.branch,sha};const r=await fetch(url,{method:'PUT',headers:{Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json','Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok){const t=await r.text();throw new Error(`PUT ${path}: ${r.status} ${t}`)}return r.json()}
export async function commitJsonFiles({token,config,message,files}){for(const [path,obj] of Object.entries(files)){const current=await ghGetFile({token,config,path});await ghPutFile({token,config,path,content:JSON.stringify(obj,null,2),message,sha:current.sha});}}
